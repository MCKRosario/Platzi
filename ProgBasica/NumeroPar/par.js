var numUsuario = prompt ('Ingresa un número');
var numUsuario = parseInt(numUsuario);

//Para obtener el módulo utilizamos >%<

if (numUsuario % 2 == 0)

{ 
    alert ('Número Par');

}

else
{
    alert ('Número Impar');
}